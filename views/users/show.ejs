<h1>Profile page</h1>

<h2><%= user.username  %></h2>

<h3>Recipes</h3>
<ul>
  <% recipes.forEach(recipe => { %>
    <li><a href="/recipes/<%= recipe._id %>"><%= recipe.name %></a></li>
  <% }); %>
</ul>
